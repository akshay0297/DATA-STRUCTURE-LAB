#include<iostream.h>
#include<conio.h>

void main ()
{
    clrscr();
    int l , i , j  , fl=1;
    char a[100] ;
    cout<<"Enter the string : ";
   cin>>a;
     for(l=0 ; a[l]!='\0' ; ++l);
         for(i=0 , j=l-1 ; i<l/2 ; ++i,--j)
         {
             if(a[j]!=a[i])
                fl=0;
         }
     if(fl==1)
        cout<<"Palindrome";
     else
        cout<<"Not Palindrome";
  getch();
}
